а) Ось код для виконання першого завдання:
people = [{"name": "John", "age": 15}, {"name": "Alice", "age": 15}, {"name": "Jack", "age": 45}]

# Знаходимо наймолодшого(-их) людину(-ей)
min_age = min(person["age"] for person in people)
youngest_names = [person["name"] for person in people if person["age"] == min_age]

print("Наймолодші люди:", youngest_names)

б) Ось код для виконання другого завдання:
people = [{"name": "John", "age": 15}, {"name": "Alice", "age": 30}, {"name": "Jack", "age": 45}]

# Знаходимо найдовше(-і) ім'я(-ена)
max_length = max(len(person["name"]) for person in people)
longest_names = [person["name"] for person in people if len(person["name"]) == max_length]

print("Найдовші імена:", longest_names)

в) Ось код для виконання третього завдання:
people = [{"name": "John", "age": 15}, {"name": "Alice", "age": 30}, {"name": "Jack", "age": 45}]

# Знаходимо середній вік
total_age = sum(person["age"] for person in people)
average_age = total_age / len(people)

print("Середній вік усіх людей:", average_age)
