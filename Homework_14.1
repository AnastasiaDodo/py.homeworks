class МаксимальнаКількістьСтудентівДосягнута(Exception):
    pass

class Група:
    def __init__(self, номер):
        self.номер = номер
        self.група = set()

    def додати_студента(self, студент):
        if len(self.група) >= 10:
            raise МаксимальнаКількістьСтудентівДосягнута("Максимальна кількість студентів досягнута в групі.")
        else:
            self.група.add(студент)

    # інші методи...

# Поза класом обробляємо власний виняток:
try:
    # Спроба додати більше 10 студентів
    for і in range(11):
        gr.додати_студента(Студент('Чоловіча', 20+і, 'Іван', f'Петрович{і}', f'АН14{і}'))
except МаксимальнаКількістьСтудентівДосягнута as e:
    print("Власний виняток перехоплено:", e)
